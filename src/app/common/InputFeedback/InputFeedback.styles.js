import { css } from "@emotion/core";
import colors from "../../styles/colors";

export const wrapper = css`
  display: flex;
  align-items: center;
  color: ${colors.thunderbird};
  font-size: 0.75rem;
  line-height: 14px;
  margin-top: 4px;
`;

export const iconFeedback = css`
  font-size: 10px;
  margin-right: 7px;
`;
